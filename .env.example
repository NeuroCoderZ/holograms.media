# .env.example
# This file documents environment variables required for the backend services, particularly Cloud Functions.
# When running locally, you can create a .env file in the same directory and load these variables.
# For Firebase Cloud Functions deployment, these variables should be configured securely via `firebase functions:config:set` or Firebase Secret Manager.

# --- Database Configuration (Neon.tech PostgreSQL) ---
# The connection string for your Neon.tech PostgreSQL database.
# This should include username, password, host, port, and database name.
# Ensure `?sslmode=require` is appended for secure connections.
# Example: postgresql://[user]:[password]@[host]:[port]/[dbname]?sslmode=require
NEON_DATABASE_URL="postgresql://your_neon_user:your_neon_password@ep-your-project-id.gcp-us-central1.neon.tech/your_db_name?sslmode=require"

# --- LLM API Configuration (Mistral AI) ---
# Your API key for accessing the Mistral AI language models (e.g., mistral-medium-latest).
# Obtain this from your Mistral AI account dashboard.
MISTRAL_API_KEY="your_mistral_api_key_here"

# --- Other potential variables (uncomment and configure as needed) ---
# GOOGLE_CLOUD_PROJECT="your-gcp-project-id" # Often automatically set in GCP, but useful for local emulation
# FIREBASE_PROJECT_ID="your-firebase-project-id"
# SOME_OTHER_SERVICE_API_KEY="your_other_api_key"
