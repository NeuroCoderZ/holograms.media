name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    # npm test шаг удален, так как он пока не актуален
    # TODO: Добавить актуальные тесты позже
