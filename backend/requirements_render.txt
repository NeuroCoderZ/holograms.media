# FastAPI & Uvicorn
fastapi>=0.115.0
uvicorn[standard]>=0.25.0

# Pydantic for data validation
pydantic>=2.10.0

# Environment variables
python-dotenv>=1.0.0

# Asynchronous file operations
aiofiles>=23.0.0

# Password Hashing & JWT (Оставляем на случай, если где-то используется)
passlib[bcrypt]>=2.0.0
python-jose[cryptography]>=3.3.0

# Firebase Admin SDK
firebase-admin>=6.0.0

# PostgreSQL driver
asyncpg>=0.25.0

# AWS SDK for Python (for S3 compatible storage like R2 or Tebi.io)
boto3>=1.20.0

# HTTP client (используется в LLMService)
httpx>=0.20.0

# Other
tenacity>=8.0.0
protobuf>=3.19.0
