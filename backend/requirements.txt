# backend/requirements.txt

# FastAPI & Uvicorn (For local development or if running a monolithic service)
fastapi>=0.115.0
uvicorn[standard]>=0.25.0

# Pydantic for data validation (General backend models)
pydantic>=2.10.0

# Firebase Admin SDK
firebase-admin>=6.0.0

# Async PostgreSQL driver
asyncpg>=0.29.0

# HTTP client
httpx>=0.24.0

# AWS SDK for Python
boto3>=1.20.0

# Environment variables
python-dotenv>=1.0.0

# Asynchronous file operations
aiofiles>=23.0.0

# Password Hashing & JWT (If custom auth is used, otherwise Firebase covers)
passlib[bcrypt]>=2.0.0
python-jose[cryptography]>=3.3.0

# LLM Clients (for direct usage, if not managed by dedicated service)
# These might be here if a local script directly calls LLMs for non-CF purposes
# For Cloud Functions, these are in llm_service.py's requirements or specific CF requirements
# mistralai # Example, uncomment if needed
# openai
# google-generativeai

# Langchain (If used for RAG or chains outside of CF)
# langchain>=0.2.0
# langchain-core
# langchain-mistralai

# Other
tenacity>=8.0.0 # For retries

# Protobuf (for NetHoloGlyph or other proto-based services)
protobuf>=3.19.0
