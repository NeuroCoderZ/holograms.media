# Используем базовый образ Node.js (выберите подходящую версию)
FROM node:18-alpine

# Устанавливаем рабочую директорию (где будет находиться код приложения)
WORKDIR /app

# Копируем package.json и package-lock.json (или yarn.lock)
COPY package*.json ./

# Устанавливаем зависимости
RUN npm install

# Копируем остальные файлы приложения
COPY . .

# Указываем порт, который будет прослушивать приложение
EXPOSE 3000

# Запускаем приложение (используем PM2, если вы его установили)
CMD ["npm", "start"]
