# Firebase Functions library
firebase-functions>=0.1.0

# Main dependency for database interaction (logging)
asyncpg>=0.29.0

# HTTP client used by LLMService
httpx>=0.24.0

# functions-framework is provided by the Google Cloud Functions environment,
# but firebase-functions is the specific Python SDK for Firebase triggers.

# Other dependencies from backend/core (like Pydantic models) are assumed
# to be deployed alongside the function or covered by a shared deployment
# that respects backend/requirements.txt.
