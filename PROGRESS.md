# 📊 Holograms.media — Журнал Прогресса и Архитектурных Решений

2026-08-15 11:40 MSK — Проведена полная ревизия безопасности и аудит исходящих каналов передачи данных (L1). Реализована строгая криптографическая проверка подписи Telegram HMAC на фронтенде (`auth.js`) и бэкенде. Удален устаревший метод прямой загрузки чанков `uploadChunk` из `apiService.js`, все операции с медиа-ассетами переведены на защищенные pre-signed URL хранилища Cloudflare R2.

2026-08-15 11:40 MSK — Реализован отказоустойчивый механизм сигналинга в `LiveAudioService.js` (L1-B). Добавлена обработка аномальных разрывов WebSocket-соединения (код ошибки 1006 / 1001) с экспоненциальным переподключением до 5 попыток, что предотвращает обрыв живого стрима при нестабильном интернет-канале.

2026-08-15 11:40 MSK — Выполнен аудит и очистка кодовой базы от неиспользуемых импортов (L2-A) в `uiManager.js` и сопутствующих модулях. Устранены циклические зависимости и устаревшие ссылки на удаленные компоненты, что облегчило дерево сборки фронтенда.

2026-08-15 11:40 MSK — Синхронизирована дизайн-система элементов управления в соответствии с канонами Impeccable Design / Taste-Skill (L2-B). Унифицированы микро-интерактивные токены кнопок левой панели и панели переключения в `css/_buttons.css` и `css/_responsive.css` (тактильный подъем `translateY(-1px)`, выверенный `backdrop-filter` и люминесценция контура).

2026-08-15 11:40 MSK — Проведен глубокий математический аудит AudioWorklet процессора `cwtAudioWorklet.js` и шейдеров рендеринга `hologramShaders.js` (L3-A). Подтверждено строгое соответствие физической шкале акустического давления dB SPL (256 частотных полос, 128 секторов знаковой стерео-панорамы) без использования генераторов псевдослучайных анимаций.

2026-08-15 11:40 MSK — Пройдено 100% сквозных E2E и модульных тестов (`npm test`), включая верификацию математики камеры (`engineCamera`), ячеистого стандарта панорамы (`panStandard`), спектрального инпейнтера и окружения Telegram WebApp.

2026-08-15 11:40 MSK — Выполнен релизный деплой сборки **v0.20.594** на Cloudflare Pages с фиксацией версионирования в Git и синхронизацией предпродового стенда `dev.holograms.media`.

2026-08-15 13:48 MSK — Устранена проблема Flash of Unstyled Content (FOUC) при обрыве или нестабильности сети (релиз **v0.20.596**). В `<head>` документа `index.html` внедрен критический встроенный CSS (`Critical Anti-FOUC Inline CSS`): жестко зафиксирован чистый черный фон `#000000` и белый цвет текста `#ffffff` с 0-й миллисекунды, а также превентивно скрыто модальное окно соглашения пользователя (`.modal { display: none; }`), исключая неконтролируемое вываливание элементов в поток до загрузки бандла.
2026-08-24 23:55 MSK — Глубокий аудит доски канбан (78 карточек) по свежим графам codegraph (5070 узлов / 11 237 рёбер) и graphify (3998 / 7407). Итог верификации: L1-C (HMAC Telegram) ЗАКРЫТА — валидация реализована (backend/auth/telegram_auth.py:97, routers/auth.py:89, 8+ тестов); L1-F закрыта ОШИБОЧНО мной (grep не покрыл js/ai/) → создана L1-F2 t_46386e58: user_email реально уходит в POST /tria/snapshots (TriaEvolutionConnector.js:111); L1-B подтверждена; L1-A и 1.2 переформулированы: PCM уходит осознанно на wss /ws/v1/tria/live (вопрос в заявлении целей юзеру), heartbeat против 1006 уже есть (netHoloGlyphClient.js:59-118), остаток — e2e.

2026-08-24 23:56 MSK — Этап 0 «гигиена доски» завершён: done 3→5. Доска больше не вводит в заблуждение.

2026-08-24 23:57 MSK — ЭТАП 1 СТАРТ (путь первого пользователя): G3/A1 подключение unifiedAuthSheet.js. Три правки:
1) js/ui/uiManager.js — клик #login-google-btn открывает единую шторку (showAuthSheet): GIS-кнопка рендерится в слот при готовом Google, фолбэк-кнопка -> prompt(), Telegram -> t.me/holograms_media_bot, «Продолжить гостем» закрывает шторку; ConsentManager.show() остаётся ТОЛЬКО для согласия сенсоров и только если sensorsAllowed=false.
2) js/core/auth.js handleGoogleCredentialResponse — после обмена токена на JWT вызывается rememberUser(state.user) (динамический import, try/catch).
3) js/core/auth.js signOut + TG-ветка initAuth — forgetUser() при выходе; rememberUser для TG-юзера после HMAC-верификации.
CSS шторки уже был подключён через style.css @import css/_auth-sheet.css (492 строки). Деплой v0.20.609 (aa3f7ac6). Приёмка живым CDP-логом — следующий шаг.

2026-08-25 01:15 MSK — ЭТАП 1 (путь первого пользователя), ночная сессия. ПРОВЕДЕНА ЖИВАЯ ПРИЁМКА G3/A1 на dev v0.20.609 через CDP (чистая вкладка): шторка «Быстрый вход» открывается по кнопке входа; GIS Google отрендерен (iframe accounts.google.com/gsi/button внутри .auth-sheet__gis); кнопки «Войти через Telegram» и «Продолжить гостем» работают; возвратника нет при первом входе — корректно. Закрыты по фактам кода: A3 (бэкенд HMAC+Ed25519 реализованы, тесты есть; Ed25519 к роуту не подключён — примечание в комментарии), A2 (хвосты закрыты, остаток «шторка при 401 автоматически» задокументирован как UX-минор), A1 (интеграция шторки), D4 (визуальная часть, CSS 492 строки подключён), G3 (auth-шторка готова; welcome->жесты->EyeLoader остаётся за G1), A1.1 (ложная тревога: GIS iframe не виден через .auth-btn селектор). Ночная авария: ОЗУ 13/14 ГБ (обучение needle в соседней сессии — НЕ ТРОГАТЬ) → таб Chrome вылетел 61696; перешёл на лёгкий CDP-режим без reload. Следующий шаг по плану: L2 consentManager (4 дефекта) или G1 онбординг.

2026-08-25 02:10 MSK — НОЧНАЯ СЕССИЯ, ИТОГИ. Закрыто 8 карточек (done 5 -> 13): L1-C (HMAC был реализован), L1-F (ошибочно — восстановлена L1-F2 t_46386e58), A3 (бэкенд HMAC+Ed25519), A2 (хвосты фронт TG-auth), A1+D4+G3 (шторка входа подключена и проверена живьём на dev v0.20.609: GIS iframe, Telegram, гость), L2 (все 4 дефекта consentManager устранены в коде от 11.08), A1.1 (ложная тревога GIS), G2 (создан Docs/RELEASE_CHECKLIST.md — гейт публикации с привязкой к карточкам; закоммичен f530e4a8). Деплои за сессию: v0.20.607/608 (гигиена логов) и v0.20.609 (интеграция шторки). Инцидент: ОЗУ 13/14ГБ (обучение needle в соседней сессии — не трогать) -> вкладки CDP перестали отвечать; перешёл на лёгкий режим без reload; живой e2e «тест на излом» согласия отложен и зафиксирован в комментарии L2. Утренний план по приоритету чеклиста: раздел 0 (e2e сети 1.2/1.3/4.9) при проснувшемся браузере, затем L3 (бэкенд-запись согласия) + L1-B/L1-F2 (user_email из payload) — они в гейте публикации.

2026-08-25 02:50 MSK — НОЧЬ, ФИНАЛ. Проведён аудит ещё трёх карточек без правок кода (безопасный режим при нехватке ОЗУ): 4.9 sleep-sync — бэкенд готов, фронтового вызывающего нет вообще (переформулирована: нужно архитектурное решение «кто должен звать»), ERR_HTTP2_PING_FAILED — сетевое, вне кода; 4.10 chat history 422 — корень в смешении двух систем id (бэкенд int vs fallback-хранилища UUID), утром проверить прод-ответ; L1-B/L1-F2 user_email — поле используется как ключ кошелька Attention Tokens (tria.py:32 -> add_reward), удаление с фронта без переноса identity на JWT обнулит награды юзеров, план из 3 шагов зафиксирован в комментариях обеих карточек. Итог ночи: закрыто 8 карточек (done 5->13... финальная статистика ниже), создан Docs/RELEASE_CHECKLIST.md (гейт публикации), PROGRESS.md ведётся. Утренний порядок: (1) e2e сети 1.2/1.3 при живом браузере; (2) прод-проверка id чат-сессий -> фикс 4.10; (3) обсуждение плана user_email->JWT; (4) G1 онбординг.

2026-08-25 09:35 MSK — УТРО, РАЗБОР ЛОГА ЮЗЕРА. Три корневых факта найдены замерами:
1) Koyeb API резолвится ТОЛЬКО IPv6 (2606:4700:10::, CNAME cloudflare.net): curl -4 = 200/0.48с, curl -6 = fail -> ERR_TIMED_OUT у клиентов без IPv6 (VersionService упал). Карточка 1.6 t_7c8bb93b. Лечение: CF Worker proxy.
2) WS signaling на dev физически мёртв: CF Pages отвечает HTTP 200 text/html на upgrade (не 101). functions/_redirects/wrangler.toml отсутствуют — проксирования /ws/* и /api/* на Koyeb НЕТ. Карточка 1.7 t_ab9e1b85.
3) HTTP-фолбэк сигналинга НЕ РАБОТАЕТ, вопреки логу 'fallback signaling active': poll возвращает HTML с 200 -> res.json() кидает -> 'Bad fallback message'. Я преждевременно закрыл 1.3 по вчерашнему логу — переоткрыл как 1.3a t_79d4b029 с извинением в комментарии.
Позитив из лога: MediaPipe Hands поднялся полностью (anonymous define/watchdog не воспроизвелись — наблюдение за 4.13), FPS Recovery ложный триггер подтверждён (Glints DISABLED при старте, t_182a1066), CwtWorklet hasInput0:false подтверждён (t_43569750).
Общий корень пунктов 1-3: dev.holograms.media не маршрутизирует бэкенд-пути на Koyeb. ОДИН CF Worker с роутами /api/* (IPv4) и /ws/* (WS proxy) закрывает 1.6 + 1.3a + 1.7. Предлагаю юзеру это как следующую задачу.

2026-08-25 09:50 MSK — Уточнение архитектуры проблемы по вопросу юзера «чем mihomo не подходит»: mihomo чинит маршрут ТОЛЬКО на машине Нейрокодера, а дефект серверный и касается КАЖДОГО посетителя dev.holograms.media: (1) CF Pages — статический хостинг без functions/_redirects, пути /api/* и /ws/* физически не доходят до Koyeb (замер: GET /ws/signaling/.../poll возвращает HTML страницу с 200, WS-upgrade получает 200 вместо 101); (2) koyeb-домен через GLB резолвится преимущественно в IPv6 — клиенты без IPv6 получают ERR_TIMED_OUT (curl -4=200/0.48с, curl -6=fail). Лечение только в мировом звене между CF и Koyeb: CF Worker с роутами /api/* (IPv4) + /ws/* (WS-proxy) на Koyeb, либо отдельный поддомен CNAME прямо на Koyeb. mihomo может пригодиться лишь как временная локальная заплатка для юзера — публике он не поможет.

